{
  "name": "just_stream_it",
  "version": "1.0.0",
  "description": "A new OpenClassroom project.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass --watch ./sass/main.scss ./public/css/style.css --style expanded --no-source-map"
  },
  "author": "Mickael",
  "license": "ISC",
  "dependencies": {
    "sass": "^1.61.0"
  }
}
